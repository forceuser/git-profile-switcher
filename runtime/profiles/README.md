# Profiles Runtime

Stores Git identity profiles and directory rules in a local JSON metadata file.
Also owns the portable import/export bundle format used for machine migration.
Profiles may include an optional shell prompt color.
